export class Item {
    name: String
    itemId: number
    price: number

    constructor(name: String, itemId: number, price: number){
        this.name = name
        this.itemId = itemId
        this.price = price
    }
}